require 'spec_helper'

describe Volt::SimpleIconSelector do
  it 'should have a version number' do
    expect(Volt::SimpleIconSelector::VERSION).not_to be nil
  end

  it 'should do something useful' do
    expect(false).to be true
  end
end
